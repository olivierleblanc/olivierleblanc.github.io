## How to run the website in local

- Open the project in `VS Code`.
- Open `Docker`.
- In `WSL`, run: 
    ```
    docker compose up 
    ```
- Navigate at `localhost:8080`.

> [!NOTE]
> No need to run again when you modify a `.md` or `.liquid` file but re-run when you modify a `.yml` file.