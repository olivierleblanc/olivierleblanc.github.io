<div class="col">
  <a href="{% if youtube.redirect %}{{ youtube.redirect }}{% else %}{{ youtube.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if youtube.img %}
        {%
          include figure.liquid
          loading="eager"
          path=youtube.img
          sizes = "250px"
          alt="youtube thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ youtube.title }}</h2>
        <p class="card-text">{{ youtube.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if youtube.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ youtube.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if youtube.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ youtube.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
